# Jess Roster

Version: v4-clean-github-no-cache

Upload the contents of this folder to the root of the GitHub repo.

This version:
- uses the device date automatically
- has no service worker
- clears old browser/PWA caches
- keeps roster codes unchanged
- runs to 31 Dec 2028

After uploading:
1. Commit changes.
2. Wait 1-2 minutes.
3. Open the GitHub Pages link with `?v=4` at the end once.
4. Delete any old Home Screen icon and add it again.
